import { TodayAppointmentsPipe } from './today-appointments.pipe';

describe('TodayAppointmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new TodayAppointmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
